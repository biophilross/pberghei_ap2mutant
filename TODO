TODO

## Build the files in the correct order

The "motif_search.Rmd" file should be built before the "motif_search_results.Rmd" file. Right now it doesn't always build it in order. Should fix this somehow.
